/*
Mini-Challenge (ES):
- Extiende tu sistema de login para incluir Refresh Tokens.
- Implementa logout seguro eliminando el Refresh Token.
- Añade middlewares de seguridad (helmet, cors, rate limiting).
- Crea un endpoint protegido que devuelva datos solo si el Access Token es válido.
Mini-Challenge (EN):
- Extend your login system to include Refresh Tokens.
- Implement secure logout by removing Refresh Token.
- Add security middlewares (helmet, cors, rate limiting).
- Create a protected endpoint that returns data only if Access Token is valid.
*/